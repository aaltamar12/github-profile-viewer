# URL of the NestJS backend used by Server Components (never reaches the client bundle).
BACKEND_URL=http://localhost:4000

# Same backend, exposed to the browser so the search-as-you-type box can call it directly.
NEXT_PUBLIC_API_URL=http://localhost:4000
